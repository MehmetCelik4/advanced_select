<%= turbo_stream.replace @target_id do %>
  <%= advanced_select_options_tag(
    target_id: @target_id,
    selected: [],
    options: @options,
    query: @query,
    add_mode: @target_id == "example_styled_remote_id_options",
    classes: (@target_id == "example_styled_remote_id_options" ? styled_advanced_select_classes : {})
  ) %>
<% end %>
